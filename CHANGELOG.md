# Changelog

All notable changes to this project will be documented in this file.

## [0.0.1] - 2025-05-27

### Added

- **认证模块** — JWT 登录、Token 刷新、API Key 绑定/删除/切换用户
- **微信读书代理** — API 网关转发（`/api/v1/weread/proxy`），支持书架、笔记、统计等数据获取
- **数据同步** — 定时同步调度器，支持全量/书架/笔记/进度同步范围，可配置频率（每小时/6h/12h/每日/每周）
- **书架管理** — 同步微信读书书架，支持全部/在读/读完/推荐筛选
- **笔记中心** — 笔记本列表 → 划线和想法浏览，支持序号显示
- **书籍详情** — 目录、笔记、点评浏览
- **笔记导出** — 支持 Markdown / HTML / TXT / PDF 多格式导出
- **阅读统计** — 仪表盘展示摘要卡片、核心指标、趋势图、分类偏好、读书排行、阅读标签，支持周/月/年/总计切换
- **全局搜索** — 顶部搜索框实时搜索微信读书书城
- **图片代理缓存** — 书籍封面本地缓存代理，首次下载后从本地返回，30 天 HTTP 缓存
- **用户资料编辑** — 支持修改昵称和上传头像
- **侧边栏用户信息** — 底部显示当前登录用户头像和昵称
- **顶部导航栏** — 面包屑导航 + 全局搜索 + 侧边栏收缩
- **深色模式** — 完整深色模式支持
- **响应式布局** — 可收缩侧边栏，移动端适配
- **健康检查** — `/api/v1/health` 和 `/api/v1/health/detail`（含版本号、运行时间）
- **应用版本号展示** — 设置页"关于"tab 动态显示后端版本号（编译注入 / VERSION 文件回退）
- **Docker 部署** — 多阶段 Dockerfile，GHCR 镜像发布工作流
- **Playwright 截图脚本** — 自动生成 README 预览截图（亮色/暗色）

### Fixed

- **API Key 重复绑定** — AES-GCM 随机 nonce 导致密文不同，改为解密后明文比对
- **头像上传后无法显示** — 静态文件路由被 `MiddlewareHandlerResponse` 包装为 JSON，拆分到独立路由组
- **头像跨域不显示** — Vite 开发模式下头像路径指向错误端口，添加 `SERVER_URL` 前缀和 Vite 代理
- **无效图片 URL** — 添加 URL 预检，跳过格式异常的微信 CDN 地址
- **同步状态卡在"进行中"** — 进程中断后 sync_logs 记录残留，自动标记超过 10 分钟的 running 记录为 failed
- **点评页显示匿名/空内容** — 修复空数据渲染问题

### Changed

- 项目名称从 WeRead Plus 重命名为 **WeRead Web**
- Logo 和 Favicon 重新设计为书本 + "+" 图标（SVG）
- 设置页重构为双栏布局（WeKeep 风格）
- 登录页添加 WeRead Skill 官方链接
- 笔记导出按钮从 Tab 栏移至书籍头部下拉菜单
- 发现页合并到书架页（推荐标签）
- 搜索页移除，改为顶部全局搜索下拉
- 统计页合并到仪表盘
- 配置文件缺失时启动自动生成默认配置
